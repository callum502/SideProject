import React,{useState} from 'react';
import ProfileFields from './ProfileFields';
export default function CompleteProfile({user,onComplete,editing=false}) {
 const [busy,setBusy]=useState(false),[error,setError]=useState('');
 return <main className="login-page"><div className="login-layout"><section className="login-card"><div className="auth-heading">{editing && <a className="auth-link" href="#explore">← Find locations</a>}<h1>{editing ? 'Your profile' : 'Complete your profile'}</h1><p>{editing ? 'Update your display name, height and ape index.' : 'Choose your display name and add your height before continuing.'}</p></div><form onSubmit={async e=>{e.preventDefault();const values=Object.fromEntries(new FormData(e.currentTarget));setBusy(true);setError('');try{const response=await fetch('/api/profile',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(values)});const data=await response.json();if(!response.ok)throw Error(data.error || 'Could not save your profile.');await onComplete(data.user);}catch(e){setError(e.message);}finally{setBusy(false);}}}><fieldset className="auth-fields" disabled={busy}><ProfileFields user={user}/>{error&&<p className="error" role="alert">{error}</p>}<button className="primary auth-submit">{busy?'Saving...':editing?'Save profile':'Continue'}</button></fieldset></form></section></div></main>;
}
