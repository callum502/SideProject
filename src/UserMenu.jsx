import React, {useState,useRef,useEffect} from 'react';
export default function UserMenu({user,logout}) {
 const [open,setOpen]=useState(false);const ref=useRef(null);
 useEffect(()=>{if(!open)return;const outside=e=>{if(!ref.current?.contains(e.target))setOpen(false);};const key=e=>{if(e.key==='Escape'){setOpen(false);ref.current?.querySelector('button')?.focus();}};document.addEventListener('pointerdown',outside);document.addEventListener('keydown',key);return()=>{document.removeEventListener('pointerdown',outside);document.removeEventListener('keydown',key);};},[open]);
 return <div className="user-menu" ref={ref}><button className="user-menu-trigger" aria-label={`Account menu for ${user.name}`} aria-expanded={open} aria-controls={open?'user-menu-options':undefined} onClick={()=>setOpen(!open)}><svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="1.8" aria-hidden="true"><circle cx="12" cy="8" r="4"/><path d="M4 22v-3a8 8 0 0 1 16 0v3"/></svg></button>{open&&<div className="user-menu-options" id="user-menu-options"><a href="#logbook" onClick={()=>setOpen(false)}>Logbook</a><a href="#details" onClick={()=>setOpen(false)}>Details</a><button onClick={()=>{setOpen(false);logout();}}>Log Out</button></div>}</div>;
}
